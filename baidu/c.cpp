#include <cstdio>
#include <cstdlib>
#include <iostream>
#include <algorithm>
#include <vector>
#include <queue>
using namespace std;

int n, k, a[100], b[100];
vector <int> r[2];

int main() {
	scanf("%d%d", &n, &k);
	for (int i = 0; i < n; ++ i)
		scanf("%d%d", a + i, b + i);
	return 0;
}

