#include <cstdio>
#include <algorithm>
#include <cstring>
#include <cmath>
#include <set>

using namespace std;

#define maxn (200000)

const int bound = 100000;
int p[maxn];
bool flag[maxn];
int n;
set<int> f;

int main() {
	for (int i = 2; i <= bound; ++i) if (!flag[i]) {
		if 
	}

	return 0;
}
